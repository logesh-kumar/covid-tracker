version: "3.7"
services:
  mongo:
    container_name: mongo
    restart: always
    image: mongo
    volumes:
      - data:/data/db
    ports:
      - 27017:27017
  api:
    container_name: api
    restart: always
    build: .
    ports:
      - 5000:5000
volumes:
  data:
